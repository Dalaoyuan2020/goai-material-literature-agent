# 触发指令 · 贝叶斯搜索层 (2026-08-11)

读 `CODEX_BAYESIAN_SEARCH.md` 并执行，不用等确认。

## 背景（不用重新查，已核实）

理论依据是两篇真实文献：
- Chitturi et al. 2024（npj Comp Mater）：贝叶斯优化在材料发现的经典应用（高斯过程+Expected Improvement）
- Liu et al. 2024（LLAMBO, arXiv:2402.03921）：LLM 接入贝叶斯优化的三个位置（热启动/代理建模/候选采样），对应官方要求的"播种/评估中间结果/剪枝聚焦"

## 核心要求

1. 新建 `src/search.py`，实现三个函数（热启动/打分/候选采样）+ 循环控制器，具体接口见 `CODEX_BAYESIAN_SEARCH.md` 第三节
2. **代理打分不是黑箱**：直接复用 `rules.py` 的 cos 平行性分数，这是本项目区别于标准 LLAMBO 的核心设计——把黑箱代理模型换成可解释的物理分数
3. 在 4 个材料家族（122/1111/11/MgB2）上各跑一次完整搜索循环，产出各自的候选和收敛记录到 `outputs/search_runs/`
4. **诚信铁律**：如果这轮没有接真实 LLM API 调用，用启发式规则模拟，必须在代码和日志里明确写"启发式近似，非真实LLM调用"，不能含糊包装成"LLM做的"
5. 跑完 `logs/log.md` 记账、写单元测试、`git push`

## 卡住怎么办

- 某个家族数据太少跑不出候选：如实记录"数据不足"，不要硬凑
- 不确定要不要真的接大模型API：先用启发式规则跑通流程，在日志里标注清楚这是近似，等确认后再决定要不要换真实API调用
