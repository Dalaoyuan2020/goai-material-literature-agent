# 触发指令 · 贝叶斯搜索 + 可视化，一次跑完 (2026-08-11)

> 给本地 Codex：一次性复制这整段发给它就能开工，不用分多条。

仓库：`https://github.com/Dalaoyuan2020/goai-material-literature-agent.git`（先 `cd` 到本地已有目录，`git pull` 拉最新；如果本地还没有就先 clone）

按顺序做两件事，第一件做完验证通过了再做第二件，不要并行改：

## 任务一：贝叶斯优化搜索层

读 `CODEX_BAYESIAN_SEARCH.md` 并执行。

**做什么**：新建 `src/search.py`，实现一个贝叶斯优化风格的搜索循环，在 4 个材料家族（122/1111/11/MgB2）上各跑一次。理论依据是 Chitturi et al. 2024（npj Comp Mater）和 LLAMBO（Liu et al. 2024, arXiv:2402.03921）里定义的 LLM 三个位置（热启动/代理建模/候选采样）。

**核心设计**：代理打分不是黑箱——直接复用 `src/rules.py` 已经算出的 cos 平行性分数，这是本项目区别于标准 LLAMBO 的创新点，要在代码注释和报告里讲清楚。

**诚信铁律（最重要的一条）**：如果这轮没有接真实大模型 API 去生成候选，只是用规则模拟"LLM 会怎么选"，必须在代码注释和 `logs/log.md` 里明确写"启发式近似，非真实 LLM 调用"，绝不能含糊包装成"LLM 做的"。这是官方红线要求的事，不能弄虚作假。

## 任务二：三类可视化

读 `CODEX_VISUALIZATION.md` 并执行。

**做什么**：新建 `src/visualize.py`，生成三类图表，全部基于 `outputs/pipeline_report.json`（和任务一新产出的 `outputs/search_runs/`）的真实数字：

1. **过程可视化**：一张 Mermaid 流程图，从文献到候选假设的完整链路，每个节点标真实数字
2. **结果可视化**：关系类型分布图 + 材料家族覆盖图
3. **方法可视化**：45组非退化证据的余弦相似度分布（可跟随机基线对比） + 一个具体类比迁移案例的示意图（A→B已知变换平移到D→D'预测候选，这张要让不懂向量数学的人也能看懂）

**贯穿两个任务的硬规则**：
- 核心证据（有DOI可溯源）和 MatKG 扩展弱证据，在代码、日志、图表里必须永远能区分开，不能混为一谈——深色/实线代表核心，浅色/虚线代表扩展，全项目统一
- 所有数字必须来自真实计算结果，不许编造或夸大
- 某个环节数据不够做不出来（比如某个材料家族样本太少），如实记录跳过理由，不要硬凑

## 卡住怎么办

同一个问题连续尝试两次仍不通过，停止修改，在 `logs/log.md` 写清楚"试了什么、卡在哪、下一步建议"，然后继续做下一个能做的部分，不要卡死不动。

## 完成后

`git add -A && git commit && git push`，提交信息写清楚这次新增了什么（几个搜索案例、几张图），不用占位符，用实际数字。
