# Codex 任务 · 三类可视化

> 给本地 Codex：读完直接开工。
> 目标：把已有的真实数据（不是虚构演示数据）变成三张图，支撑调研报告。

## 为什么是这三类，不是别的

按吕博士的要求，三类可视化对应三件不同的事，不要混着做成一张万能图：

1. **过程可视化** —— 讲清楚"我们是怎么走到这一步的"，给读报告的人一条时间线/流程线
2. **结果可视化** —— 讲清楚"我们现在手上有什么"，是数据本身的分布/规模
3. **方法可视化** —— 讲清楚"我们验证的这个假设，证据长什么样"，是最能体现严谨性的一张，也是最容易被审稿人盯着看的一张

## 一、过程可视化：知识库构建流程图

**内容**：一张流程图，展示从"原始文献"到"最终候选假设"的完整链路，标出每一步的真实产出数字。

```
Sciverse检索 + 人工核验(20篇文献)
        ↓
knowledge/edges.csv (36条核心边, 35个材料点)
        ↓ [MatKG扩展，弱证据]
knowledge/edges_matkg.csv (210条扩展边, 47个扩展节点)
        ↓
src/graph.py 向量化 (82个点, 33维成分空间)
        ↓
src/rules.py 平行性检测 (45组非退化真实证据)
        ↓
src/application.py 类比迁移 (4个候选假设)
        ↓
src/search.py 贝叶斯搜索 (4个材料家族案例)
        ↓
调研报告
```

**做法**：用 Mermaid 流程图（flowchart TD），每个节点标真实数字，不要用占位符。核心集和扩展集用不同颜色区分（比如核心用实线深色，MatKG扩展用虚线浅色），呼应"强证据 vs 弱证据"这条贯穿全项目的规则。

**产出**：`outputs/visualizations/01_pipeline_flow.md`（Mermaid 源码）+ 用 mermaid-cli 或等效工具渲染出的 PNG。

## 二、结果可视化：知识库规模与分布

这是"我们现在有什么"，建议至少两张图：

**2a. 关系类型分布图**（柱状图）
- X轴：R1-R9九类关系 + MKG-DOPING/SUBSTITUTION/SYNTHESIS/STRUCTURE四类弱关系
- Y轴：条数
- 核心集和MatKG扩展集用不同颜色的柱子并排放，一眼看出"强证据"和"弱证据"在各关系类型上的分布差异

**2b. 材料家族覆盖图**（可以是饼图或者柱状图）
- 按 `graph.py` 里的 STRUCTURE_FAMILIES（122/1111/11/214/diboride/other）分类
- 展示每个家族里有多少材料点、多少边——这张图直接回答"我们的知识库覆盖了超导材料的哪些主要家族，覆盖得够不够均匀"

**产出**：`outputs/visualizations/02a_relation_distribution.png`、`02b_family_coverage.png`

## 三、方法可视化：平行性证据与类比迁移演示图

这张最重要，是"我们的方法有没有真的成立"的直接证据，建议两个部分：

**3a. 关系向量平行性散点/网络图**
- 把找到的45组非退化证据可视化：可以做成一个简化的向量空间投影（比如用PCA或者t-SNE把高维的边向量降到2维），同一类关系的边向量应该聚在相近的方向上
- 如果降维图做起来复杂，退而求其次：至少做一张"余弦相似度分布直方图"，展示这45组非退化证据的cos值分布，跟随机基线对比（如果之前算过随机基线的话，那晚的记录里提到过"随机基线mean=+0.002±0.422"，这个可以拿来对比画在同一张图上）

**3b. 一个具体类比迁移案例的示意图**（这张最直观，建议优先做）
- 挑一个已有的候选假设（比如"BaFe2As2→BaFe1.8Co0.2As2"这个变换，类比到某个目标母体）
- 画三个节点：源材料A、源材料B（已知变换）、目标材料D（迁移目标），用箭头表示"变换向量"从A→B，再平移到D→D'（预测的候选）
- 这张图要能让一个不懂向量数学的人，一眼看懂"我们是怎么从已知推出候选的"

**产出**：`outputs/visualizations/03a_parallelism_distribution.png`、`03b_analogy_case_demo.png`

## 技术选型

- 优先用 Python 标准可视化库（matplotlib），不引入新的重依赖，跟现有代码风格一致（`src/` 目前是零第三方依赖，如果画图必须用 matplotlib/numpy，这两个可以作为唯一的例外依赖，写清楚在 README 或 CLAUDE.md 里）
- Mermaid 流程图不需要额外依赖，直接写 `.md` 文件里的代码块即可，很多平台能原生渲染（包括GitHub）
- 所有图表配色遵循一个原则：**核心证据用深色/实线，扩展/弱证据用浅色/虚线**，全项目统一，不要每张图自己发明一套配色

## 产出汇总

新建 `src/visualize.py`，运行后一次性生成上述所有图表到 `outputs/visualizations/`。

在 `logs/log.md` 追加记录：生成了几张图、每张图对应哪个真实数据来源，如果某张图因为数据不够没法做（比如降维图如果材料点太少做出来没有意义），如实记录跳过的理由。

## 铁律

1. **图表数据必须来自 `outputs/pipeline_report.json` 或 `outputs/search_runs/` 里的真实产出，不能为了好看去编造或夸大数字**
2. 核心证据和扩展弱证据在任何图里都要能区分开，不能视觉上混为一谈（这是延续整个项目最重要的一条规则）
3. 图表要配简短的图注（一两句话说清楚这张图在讲什么），不要只给一张没有上下文的图
