# 科研 Agent 工作台 · 前端开发说明书

| | |
|---|---|
| **文档类型** | 产品说明书 · 开发规格 |
| **产品经理** | 致远 |
| **开发** | 谭力奎 |
| **版本** | v1 · 2026-08-11 |

照 Codex 的路子做一个能下载到本地跑的桌面 UI，三栏布局，先把壳做出来能用，功能后续陆续接。

**目录**：[01 分工与边界](#01-分工与边界) · [02 页面长什么样](#02-页面长什么样) · [03 字库面板详解](#03-字库面板详解--这个案例说清楚卡帕西知识库是什么) · [04 接口契约](#04-接口契约--四个端口)

---

## 01 分工与边界

```
   产品经理                      开发
   ────────                     ────────
   致远              →           谭力奎
   功能 / 算法 / 知识库          UI / 交互 / 前端工程
   不碰界面                      这是他自己的项目产出
```

### V1 只做什么，不做什么

| 模块 | V1 范围 | 暂不做 |
|---|---|---|
| 整体框架 | 三栏布局能跑起来，能下载到本地打开 | 打包分发、自动更新 |
| 文献阅读器 | 能预览 PDF/PPT 的空壳 | 真实解析、划词翻译、批注 |
| 字库面板 | 只读展示，读真实数据渲染 | 编辑/新增/删除条目 |
| 编排 / 技能 | 两个按钮能点开，界面占位 | 真正调用后端执行 |
| 对话区 | 输入框 + 消息气泡样式 | 真实模型接入 |

---

## 02 页面长什么样

三栏，从左到右：**导航栏**（窄，图标式）→ **工作区**（宽，主对话/内容区）→ **字库面板**（固定宽度，右侧常驻）。参考 Codex 开源界面的整体气质，不追求视觉花哨。

```
┌──────┬──────────────────────────────────┬─────────────────────┐
│      │  工作区 · 编排                    │  字库                │
│ 编排 │                                    │                     │
│ 技能 │  帮我找一下超导材料里，122体系     │  材料点 [核心]   91  │
│ 字库 │  掺钴之后还有哪些没做过的组合      │  材料点 [扩展]   46  │
│      │                                    │  关系边 [核心]   79  │
│      │  正在检索知识库中的122体系边...    │  关系边 [扩展]  210  │
│      │  已找到3组已验证变换，正在生成候选 │  候选假设         6  │
│      │                                    │                     │
│      │  候选：SrFe2As2 → SrFe1.8Co0.2As2  │                     │
│      │  （来源余弦0.9958，未验证，需交叉  │                     │
│      │  核实）                            │                     │
└──────┴──────────────────────────────────┴─────────────────────┘
```

> 这是示意图，不是最终视觉稿——重点是结构比例和信息层级，配色/字体谭自己定。图里的 91 / 46 / 79 / 210 / 6 是当前项目的真实数字，不是随手编的占位数。

---

## 03 字库面板详解 —— 这个案例说清楚"卡帕西知识库"是什么

**字库面板不是一个通用的"文件管理器"**，它展示的是一套特定结构的数据：材料科学文献被拆解成"点"（材料）和"边"（材料之间的关系），每条边都能追溯到具体论文。这套结构现在已经在跑，V1 只需要**把这份真实数据读出来、按下面这个样子摆出来**，不用设计新的信息架构。

### 点开一条边，应该看到什么

| 字段 | 值 |
|---|---|
| 材料 A | LaFeAsO |
| 材料 B | LaFeAsO1-xFx |
| 关系类型 | R4 · 氧位F掺杂 |
| 文献 DOI | `10.1103/PhysRevB.78.020512` |
| 年份 | 2008 |
| 证据强度 | direct |
| 证据摘录 | "The F doped LaFeAsO, a recently discovered superconductor with the high Tc of 26 K" |

这条数据是真实存在的，来自项目仓库的 `knowledge/edges.csv` 第一行。字库面板点开任意一条边，都应该长这样——不是占位假数据，是把这个文件真实读出来渲染。

### 核心 vs 扩展——两种数据不能视觉混淆

系统里有两批数据，可信度不一样，**UI 上必须能一眼分清**：

| 类型 | 说明 |
|---|---|
| 核心数据 | 每条边都有真实 DOI，人工核验过，91个点 / 79条边 |
| 扩展数据 | 来自公开知识图谱批量导入，没有逐条核验，46个点 / 210条边，仅用于辅助参考 |

V1 做法很简单：两种数据加一个小标签区分（比如上面示意图里的"核心/扩展"标签），不用做复杂的可视化，能让人一眼看出"这条数据能不能直接信"就够了。

---

## 04 接口契约 —— 四个端口

后端按这四个文件跟前端对接，字段名和层级现在已经定了，谭直接按这个格式读文件渲染，不需要理解后端具体怎么算出来的。

### 1 · 记忆 —— 字库数据从哪读

```json
// knowledge/edges.csv 每行一条边
{
  "材料A": "LaFeAsO",
  "材料B": "LaFeAsO1-xFx",
  "关系类型": "R4",
  "文献DOI": "10.1103/PhysRevB.78.020512",
  "年份": 2008,
  "证据强度": "direct",
  "证据说明": "..."
}
```

对应上面第03节展示的那条边。扩展数据在 `knowledge/edges_matkg.csv`，字段名不同，前端按同一个视觉规则打标签即可，不用理解字段差异的原因。

### 2 · 技能 —— 概览数字从哪读

```json
// outputs/pipeline_report.json
{
  "L2_structure": {
    "core_materials_count": 91,
    "core_edges_count": 79,
    "extended_nodes_count": 46,
    "extended_edges_count": 210
  },
  "L4_application": {
    "candidates_generated": 6
  }
}
```

字库面板右侧那几个统计数字（示意图里的91/46/79/210/6），直接读这个文件的对应字段，不用自己算。

### 3 · 灵魂 —— 身份定义

静态文件，定义这个 Agent 是谁、语气风格、边界。V1 阶段前端不需要解析内容，只要有这个文件存在、能被工作流读取即可，具体内容后续再补。

### 4 · 工作流 —— 对话怎么触发后端

V1 阶段对话区可以先做成界面样式（输入框+气泡），不用真的接通后端执行——等后端这边把执行逻辑理清楚了再对接，这次先把壳做扎实。

---

## 验收标准

- [ ] 三栏布局能在本地打开，导航栏三个按钮能切换（不要求功能，要求能点、有视觉反馈）
- [ ] 字库面板读取 `edges.csv` 真实数据，展示出至少几条真实边（不是硬编码假数据）
- [ ] 核心数据和扩展数据视觉上能区分（一个小标签即可，不需要复杂设计）
- [ ] 点开任意一条边，能看到第03节展示的那七个字段
- [ ] 整体安装包/运行方式简单，能打开就算过关，不追求长期无 bug

---

图例：**核心数据**（DOI可溯源） · **扩展数据**（辅助参考）
