# Codex 工作流 · 自动构建文献知识库

> 给本地 Codex：读完本文件直接开工，不用等人确认。这是一个可以反复跑的循环，不是一次性任务。
> 背景 schema 见 `CLAUDE.md`，本文件只讲"怎么跑起来"。

## 目标（就三件事，别整复杂）

1. **结构**：文献 → 点（材料）+ 边（关系）
2. **距离**：两个点/两条边之间怎么算相似、怎么算平行
3. **运算规则**：拿距离去干什么——发现哪些边是同一类变换、能不能类比迁移出新东西

这三件事已经在 `src/graph.py`（结构）、`src/rules.py`（距离/平行性）、`src/application.py`（运算规则/类比迁移）里实现了一版最小可跑的。**Codex 的任务不是重新发明，是把"结构"这一步从"人工核验 36 条"扩展成"自动化持续入库"。**

## 每一轮要做的事（可以循环跑，跑多少轮自己判断）

### 第一步：检索（用 src/sciverse.py）

调用 `meta_search()`，换不同关键词查超导材料相关文献（比如 "iron pnictide doping"、"cuprate substitution"、"pressure induced superconductivity" 这类）。每次调用会自动落盘审计记录到 `outputs/sciverse_calls/`，不用额外处理。

### 第二步：抽取（新增到 knowledge/edges.csv）

从检索结果的标题+摘要里，抽取符合现有 schema 的新关系记录：

```
材料A,材料B,关系类型,关系类型定义,文献DOI,年份,证据强度,文献标题,证据说明
```

**铁律**：
- 关系类型必须落进已有的 R1-R9 分类（定义见 `CLAUDE.md`），实在归不了类的先跳过，不要发明新类别（除非发现明显是新的一大类模式，那就在 `logs/log.md` 里提出来，等人确认再定）
- 证据说明必须是摘要里摘录的原文片段，**不许自己编一句话概括**——抽不出真实摘录的这条边不入库
- 只追加，不要覆盖已有的 36 条

### 第三步：编译（跑 pipeline）

```bash
cd src && python3 pipeline.py
```

这会自动用新的 `edges.csv` 重新算点的向量、边的平行性、生成新的类比迁移候选，写进 `outputs/pipeline_report.json`。

### 第四步：记账

在 `logs/log.md` **追加**一条（不要改历史记录）：
- 日期
- 这轮检索用了什么关键词
- 新增了几条边
- 平行性证据/候选假设数量有没有变化
- 有没有发现值得注意的新模式（比如某类关系数据特别多/特别少）

### 第五步：推送

```bash
git add -A
git commit -m "chore: expand knowledge base, +N edges from <关键词>"
git push
```
推送前确认没有把任何密钥文件加进去（本仓库本来就不该有密钥，`.env` 之类的不要加）。

## 什么时候算这一轮跑完了

跑够你觉得合适的关键词数量就停（比如 5-8 个方向），不用一次跑到把所有超导材料文献都覆盖——这是个能反复跑的循环，下次开工接着跑，`logs/log.md` 会告诉下一轮从哪继续。

## 已知的技术债（不用现在处理，知道就行）

- 边的方向性还没分类（有方向的"变换边" vs 无方向的"相似边"），R9 这种同结构家族目前会被自动排除在平行性计算之外，是正确行为，不是 bug
- MatKG（`github.com/olivettigroup/MatKG`，CC0协议现成材料知识图谱，184MB的 SUBRELOBJ.csv）还没评估能不能直接接入，如果第一步检索效率不够，可以考虑改成直接用 MatKG 的数据

## 别做的事

- 不要重写 `src/` 里的核心逻辑（graph.py/rules.py/application.py），除非发现明确的 bug
- 不要引入新的依赖库，这套东西现在零依赖（只用 python 标准库），保持这样
- 不要为了"看起来完整"去编造证据说明或者关系类型
