# 触发指令 · 可视化 (2026-08-11)

读 `CODEX_VISUALIZATION.md` 并执行，不用等确认。

## 三类可视化，缺一不可

1. **过程可视化**：一张 Mermaid 流程图，从原始文献到最终候选假设的完整链路，每个节点标真实数字
2. **结果可视化**：两张图（关系类型分布、材料家族覆盖），展示知识库现有规模和分布
3. **方法可视化**：两张图（平行性证据分布、一个具体类比迁移案例的示意图），展示方法本身是否成立，这是最重要的一组

## 三条硬要求

1. **所有数据来自 `outputs/pipeline_report.json` 真实产出，不许编造或夸大数字**
2. **核心证据（可溯源DOI）和 MatKG 扩展弱证据，在所有图里必须视觉上能区分开**——深色实线 vs 浅色虚线，全项目统一配色，这是延续项目最重要的一条规则
3. 只用 matplotlib 作为唯一新增依赖（如果需要的话），不要引入其他重依赖；Mermaid 流程图不需要额外依赖

## 产出

`src/visualize.py` + `outputs/visualizations/` 下 5 张图（或 Mermaid 源码+渲染图）+ `logs/log.md` 记账 + `git push`

某个图因为数据不够做不出来，如实记录跳过理由，不要硬凑。
