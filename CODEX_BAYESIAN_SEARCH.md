# Codex 任务 · 贝叶斯优化搜索层设计与实现

> 给本地 Codex：读完直接开工，不用等确认。
> 这是给现有 L4（`src/application.py`）加一层搜索控制器，对应官方进阶路线A的硬要求：
> "搜索方法需与LLM深度融合，不能只在最后写总结"。

## 一、理论依据（已核实，不用再查，直接引用）

1. **贝叶斯优化在材料发现里的经典应用**：Chitturi et al. 2024, *Targeted materials discovery using Bayesian algorithm*, npj Computational Materials (被引43次)。核心组件：高斯过程代理模型 + Expected Improvement 采集函数，用少量实验逼近最优材料配方。
2. **LLM 接入贝叶斯优化的三个位置**：Liu et al. 2024, *Large Language Models to Enhance Bayesian Optimization (LLAMBO)*, arXiv:2402.03921。定义了 LLM 在 BO 循环里的三个角色：
   - **热启动（Zero-Shot Warmstarting）**：LLM 用自然语言理解问题，提出有希望的初始候选点
   - **代理建模（Surrogate Modeling）**：LLM 根据历史观察，判断新候选"大概怎么样"，部分替代传统高斯过程
   - **候选采样（Candidate Sampling）**：LLM 结合历史结果和高层问题背景，提出下一批候选，兼顾探索与利用

这三个位置和官方要求的"播种/评估中间结果/剪枝聚焦"三环节一一对应，不是巧合，是同一套思路的两种表述。

## 二、我们的设计（跟标准 LLAMBO 的差异，这是本项目的亮点）

标准 LLAMBO 用高斯过程或 LLM 直接预测数值当代理模型，是黑箱拟合。**我们不这么做**——我们已经有 `src/rules.py` 算出来的 cos 平行性分数，这个分数**有物理意义**（同一种材料变换在不同母体上是否稳定），直接拿它当代理信号，不用再训练一个黑箱模型。

**这是本项目区别于标准 BO/LLAMBO 应用的核心创新点**：把"黑箱代理模型"换成"可解释的物理分数"，更贴合官方红线③"拒绝黑箱"。写系统说明的时候这条要讲清楚，不要含糊带过。

## 三、要实现什么（新文件 `src/search.py`）

### 循环结构

```
for round in range(max_rounds):
    候选生成（LLM热启动/采样）→ 打分（cos平行性，来自rules.py，不是黑箱）→
    LLM判断（结合分数+化学常识，如Co/Mn反例这类已知边界）→ 记录/剪枝 →
    收敛检测（连续K轮无新发现则停）
```

### 三个函数，对应 LLAMBO 的三个位置

1. `warmstart_candidates(relation_type, materials, els, families) -> list`
   - 对应"热启动"。基于现有 `application.find_analogy_source()` 的结果，让 LLM（或规则近似，如果这轮不接真实LLM调用）从已知非退化平行性证据里，挑几个"看起来最值得深挖"的关系类型作为起点
   - 不要求真的调用大模型API（除非 Codex 判断本机有可用的 LLM 调用能力），可以先用一个明确的启发式规则模拟"LLM会怎么选"，但**必须在代码注释和日志里明确标注"这是启发式近似，不是真实LLM调用"**，不能含糊说成"LLM做的"

2. `score_candidate(candidate, vecs, comp_dims) -> float`
   - 对应"代理建模"，但**不是黑箱**——直接复用 `rules.cosine()` 算出的平行性分数，分数越高说明这个候选变换和已知模式越吻合
   - 加一条：如果候选落在已知的"退化"或者"Co/Mn式反例"模式里，分数要打折扣，这条要显式写出打折的理由

3. `propose_next_round(scored_candidates, history) -> list`
   - 对应"候选采样"。从上一轮打分结果里，选出还没探索过的、分数有潜力的方向，作为下一轮候选
   - 加一条 LLAMBO 强调的规则：**已经观察过的候选不能重复推荐**（`已经观察过的值不能再推荐`这条约束直接抄）

### 收敛条件（复用之前设计过的"跑到干为止"逻辑）

连续 2 轮没有新的非退化候选出现，就停止；总轮数上限设 5 轮，超过也停。停止时输出：跑了几轮、每轮候选数、最终收敛还是撞了轮数上限。

## 四、案例设计：3-4个材料家族，同一套搜索逻辑各跑一次

不是"3-4种算法"，是**同一套搜索逻辑，在3-4个不同材料家族上各跑一次**，体现"深度打透+覆盖主要家族"：

| 家族 | 对应结构标签（`graph.py` 里的 STRUCTURE_FAMILIES） | 备注 |
|---|---|---|
| 122 体系 | `122` | 数据最多，优先跑 |
| 1111 体系 | `1111` | |
| 11 体系（FeSe类） | `11` | |
| MgB2 体系 | `diboride` | 数据可能较少，如果候选生成不出结果，如实记录"该家族数据不足以支撑搜索"，不要硬凑 |

每个家族跑一次 `search.py` 的完整循环，产出各自的候选假设列表和收敛过程记录。

## 五、产出

- `src/search.py`：三个核心函数 + 循环控制器
- `outputs/search_runs/<家族名>.json`：每个家族的完整搜索记录（每轮候选、打分、是否收敛）
- `logs/log.md` 追加：4个家族各跑了几轮、各产出几个候选、有没有家族数据不够跑不出来
- 单元测试：至少覆盖"已观察候选不重复推荐"和"收敛条件触发"这两条

## 六、铁律

1. **不要把启发式规则包装成"LLM做的"**——如果这轮没有真实调用大模型API，代码注释和日志都要如实写"启发式近似"，这是诚信问题，不是实现细节
2. **打分必须来自 `rules.py` 的真实 cos 计算，不能编数字**
3. **每个候选最终都要标注"候选假设(未验证)"，不能包装成结论**——这条和 `application.py` 已有的规则一致，延续下去
4. 不确定的地方（比如某个家族数据太少跑不出结果）如实记录，不要为了"看起来完整"硬凑
