# Codex 启动 Prompt（完整版，可直接复制使用）

> 这是给本地 Codex 的**触发 prompt**，把它整个复制粘贴给 Codex 开工。
> 下面每个决策点都写了"怎么判断、判断错了怎么办"，减少它自己拍脑袋的空间。

---

## 你的任务

仓库：`https://github.com/Dalaoyuan2020/goai-material-literature-agent.git`（如果本地还没有，先 `git clone`；已有就 `git pull` 拉最新）。

读仓库根目录三个文件，按顺序：
1. `CLAUDE.md` —— 这个仓库的 schema 契约，点/边/证据链怎么定义
2. `CODEX_MATKG_INTEGRATION.md` —— 你现在要做的具体任务（优先做这个）
3. `CODEX_WORKFLOW.md` —— 备选任务（MatKG 这条走不通时切到这个）

**目标只有一句话**：把现成的 MatKG 材料知识图谱里跟超导相关的部分，缝进现有的 `knowledge/edges.csv`/`edges_matkg.csv` 体系里，让点和边的规模从 35/36 扩大一个数量级以上，同时不破坏"每条结论可溯源"这条硬约束。

## 决策点 1：MatKG 下载失败/太大跑不动怎么办

- 先试 `curl -L -o /tmp/SUBRELOBJ.csv "https://zenodo.org/records/10022727/files/SUBRELOBJ.csv?download=1"`
- 如果超时或者网络不通：换成分块下载（`curl -C -` 断点续传），重试最多 3 次
- 如果 3 次都失败：**不要卡在这里**，改跑 `CODEX_WORKFLOW.md` 那条路线（用 Sciverse 一点点检索抽取），并在 `logs/log.md` 里记一条"MatKG 下载失败，原因是 X，已切换到检索抽取路线"
- 如果下载成功但 184MB 的 csv 用 pandas 读不动/太慢：改用逐行流式读取（Python 内置 `csv` 模块，不要一次性 `read_csv` 全加载进内存）

## 决策点 2：MatKG 的列名/格式跟预期不一样怎么办

不要假设列名是什么，**先 `head -5` 看真实格式再写解析代码**。大概率是三元组形式（subject, relation, object 或类似命名），但具体字段名以实际下载的文件为准。

- 如果格式完全看不懂：把前 20 行原样贴进 `logs/log.md`，标注【待人工确认格式】，先跳过这一步，去做筛选逻辑的设计（可以先用假设的格式写代码框架，格式确认后再接上）
- 不要花超过 20 分钟去猜格式，猜不出来就如实记录卡在哪，不要编造一个格式硬套

## 决策点 3：怎么判断一条 MatKG 三元组"跟超导相关"

用两层过滤，都满足才算相关：
1. **材料层过滤**：三元组里出现的材料实体，要么是 `knowledge/edges.csv` 里已有的 35 个化学式之一，要么包含常见超导材料家族关键词（Fe-based / cuprate / pnictide / 122 / 1111 / MgB2 这类）
2. **关系层过滤**：关系类型要落在跟 R1-R9 体系相关的范畴内（掺杂 doping / 取代 substitution / 合成 synthesis / 结构 structure），纯粹"材料-应用场景"这种跟我们的边定义无关的关系，不要硬塞进来

如果筛出来的数据量太小（比如少于 50 条）：说明超导材料在 MatKG 里的覆盖率本来就低，如实记录这个发现，不要放宽过滤条件去凑数量。

## 决策点 4：MatKG 数据没有 DOI，能不能算"有证据"

**不能。** 这是本任务最容易踩的坑，专门强调一遍：

- `CLAUDE.md` 定义的证据链要求是"每条边有真实DOI+摘录证据"，这是官方红线①的硬约束，不能因为想扩大规模就放松
- MatKG 提供的是"实体+关系"，通常没有直接给出支撑这条关系的具体文献DOI（如果 MatKG 数据里其实有类似 doi 或 paper_id 的字段，那就是有证据的，直接按可溯源处理；如果没有，就是下面这种情况）
- 没有 DOI 的这部分数据，**必须**存进单独的 `edges_matkg.csv`，不能混进 `edges.csv`
- 在最终的调研报告/系统说明里，这两类数据要分开讲清楚："核心集36+条边，人工逐条核验，可精确溯源到具体文献；扩展集N条边，来自 MatKG（Venugopal & Olivetti 2024 公开数据集），置信度较低，用于扩大向量空间规模，不单独作为强证据引用"

## 决策点 5：接入后 L3/L4 的代码要不要改

`src/graph.py` 的 `build_graph()` 现在只读一个文件。你需要让它能读两个文件（核心集+扩展集），但**在计算平行性证据（L3）的"非退化真实证据"这个最终判断时，只用核心集**——扩展集的边可以用来扩大向量空间/辅助判断趋势，但不能作为"真实证据"直接写进报告里当结论支撑，因为它没有逐条核验过。

这个区分很重要，改代码时候留一个参数区分"用什么规模的图做向量空间"和"引用什么规模的边当强证据"，不要把两件事焊死在一起。

## 完成标准

跑完 `src/pipeline.py`，能看到：
- 点的数量、边的数量相比接入前明显增长
- `logs/log.md` 有完整记录：MatKG 处理了多少条原始数据、筛出多少条相关的、格式转换遇到什么问题、最终扩展集有多少条边
- `git push` 成功，提交信息写清楚这次变化的具体数字

## 卡住了怎么办（总原则）

任何一步卡住超过预期，**不要沉默硬扛，也不要跳步硬凑结果**。做法统一是：在 `logs/log.md` 里如实记录"卡在哪、试了什么、下一步建议是什么"，然后转去做下一件能做的事（比如决策点1里说的，MatKG走不通就切到检索抽取路线），不要让整个任务卡死不动。
