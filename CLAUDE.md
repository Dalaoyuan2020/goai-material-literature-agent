# CLAUDE.md — 材料科学文献知识库

本仓库 = 一个**文献知识库**，不是一个要交付的比赛产品。GOAI 比赛是探针，逼出这块拼图；知识库本身才是要留下的东西。

## 你是谁

你是这个知识库的编译器。原料是文献（只读，不可篡改），知识库是你从原料编译出来的结构化产物。

## 三层结构（跟你在别的地方看到的"Kiwi/Karpathy LLM Wiki"是两回事，别套错схема）

| 层 | 目录 | 内容 | 谁写 |
|---|---|---|---|
| 原料层 | `raw/` | 原始文献（PDF/摘要/元数据），只读，不可改 | 检索/下载脚本写入，写完不再动 |
| 知识层 | `knowledge/` | 从原料抽出的结构化知识：材料(点)、关系(边) | 编译器(你)写入 |
| 运算层 | `src/` | 点边向量化、平行性检测、类比迁移的代码 | 人工写的固定代码，不是编译产物 |

**关键区别**：Kiwi 那套是"论文→给人看的维基页面"（摘要页/概念页/实体页）。这里是"论文→给程序算的结构化数据"（材料/关系/证据）。目的不同，schema 不同，不要混用。

## 知识层 schema（当前版本，v1）

`knowledge/edges.csv`：每一行是一条边。

| 列名 | 含义 |
|---|---|
| 材料A / 材料B | 边的两个端点(点) |
| 关系类型 | R1-R9，定义见下 |
| 关系类型定义 | 人类可读的说明 |
| 文献DOI | 支撑这条边的原始文献 |
| 年份 | 文献发表年 |
| 证据强度 | direct / secondary |
| 文献标题 | — |
| 证据说明 | 摘录自文献的原文片段，防止编造 |

**点的定义**：一个点 = 一个材料的化学式（含掺杂状态），向量 = 成分比例 + 结构家族 one-hot，详见 `src/graph.py`。

`knowledge/edges_matkg.csv`：MatKG 自动抽取数据的**低置信度扩展集**，与核心集物理分离。其端点可包含材料、合成方法、结构/相标签等异构实体；关系类型使用 `MKG-*` 命名并保留 MatKG 原始 NER 标签与聚合共现次数。`SUBRELOBJ.csv` 不保留逐行 DOI，因此扩展集的 `文献DOI` 为空、`强证据资格=false`。它只能用于扩大图/向量空间和观察趋势，不能作为 L3「非退化真实证据」或报告中的强结论引用。

扩展集由标准库流式转换器生成，不把 184MB CSV 一次性载入内存：`python src/matkg.py --input <SUBRELOBJ.csv>`。转换器先做材料家族过滤，再做掺杂/取代/合成/结构关系过滤；筛出少于 50 条时也不得放宽条件凑数。

核心集与扩展集的读取必须由参数显式区分：`build_graph(include_extended=True)` 可构建核心+扩展向量空间；L3/L4 的证据来源必须显式使用 `load_core_edges()`。不得仅因 MatKG 的 `Count` 较高就把聚合共现当作可溯源的因果/变换证据。

## 搜索与可视化

- `python src/search.py` 在 122/1111/11/MgB2 四个家族运行逐轮扩张的可解释搜索控制器。候选排序和轮次扩张决策依次尝试 STEP、Gemini，并在两者不可用时明确降级为启发式；每个逻辑调用都审计到 `outputs/llm_calls/`，报告的 `real_llm_api_called` 如实记录是否成功使用真实 API。数值信号直接复用 `rules.cosine` 的核心边变换平行性；没有后验分布或 Expected Improvement，因此不称为贝叶斯优化。
- `E:\Anaconda3\envs\camel_agent\python.exe src/visualize.py` 从 `outputs/pipeline_report.json` 和 `outputs/search_runs/` 生成流程、分布与方法图。该环境使用相容的 NumPy 1.26.4 / Matplotlib 3.10.1；不要使用当前同时残留 NumPy 1.x/2.x 文件的 `E:\Anaconda3\python.exe` base 环境。可视化是仓库中唯一允许使用第三方库的部分；计算主管线和搜索层仍使用 Python 标准库。
- 全项目统一视觉语义：核心 DOI 证据使用深色/实线，MatKG 聚合弱证据使用浅色/虚线或阴影；候选假设使用橙色虚线并明确标注“未验证”。

**关系类型体系（R1-R9）**：
- R1 阳离子位掺杂 R2 Fe位取代掺杂 R3 阴离子位等电子取代 R4 氧位F掺杂
- R5 稀土位替换 R6 界面/衬底增强 R7 压力诱导超导 R8 Tc记录超越 R9 同结构家族(无方向)

## 入库流程

1. 检索(`src/sciverse.py`) → 落 `raw/`，并在 `outputs/sciverse_calls/` 留调用审计记录
2. 从 `raw/` 抽取关系 → 追加进 `knowledge/edges.csv`，**每条边必须有文献DOI和证据说明摘录，不许编造**
3. 追加 `logs/log.md`（append-only，记录本次入库了什么、抽了几条、有没有发现编码退化的假信号）

## 铁律

1. **不编造**：抽不出证据说明摘录的边不入库，标【待核实】
2. **原料层只读**：`raw/` 一旦写入不再修改，要更正就在 `knowledge/` 层标注
3. **点边定义变更要走 CLAUDE.md**：改 schema 就更新本文件，不要让 schema 隐式漂移
4. **私有仓库**：这是内部知识库，不进行公开开源宣传，比赛提交是附带产物不是目标
